services:
  node:
    build:
      context: ..
      dockerfile: dev/Dockerfile
    volumes:
      - ..:/app
      - node_modules:/app/node_modules
    working_dir: /app
    command: ["sh", "-c", "tail -f /dev/null"]

volumes:
  node_modules:
